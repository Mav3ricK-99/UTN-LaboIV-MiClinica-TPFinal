import { CastToEspecialistaPipe } from './cast-to-especialista.pipe';

describe('CastToEspecialistaPipe', () => {
  it('create an instance', () => {
    const pipe = new CastToEspecialistaPipe();
    expect(pipe).toBeTruthy();
  });
});
