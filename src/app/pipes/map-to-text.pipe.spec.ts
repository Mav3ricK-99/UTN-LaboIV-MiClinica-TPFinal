import { MapToTextPipe } from './map-to-text.pipe';

describe('MapToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
